'use strict';

/**
 * publication-strategique service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::publication-strategique.publication-strategique');
